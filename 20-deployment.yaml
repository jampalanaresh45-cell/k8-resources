apiVersion: apps/v1 
kind: Deployment
metadata:
  name: nginx
  ########### Replica set labels ###############
  labels: 
    purpose: dp-demo
    project: roboshop
  
spec:
  replicas: 4
  selector:
  ######### These are used to select pods ##############
    matchLabels:
      purpose: dp-demo
      project: roboshop
### pod definition ########

  template:
    metadata:
      labels:
        purpose: dp-demo
        project: roboshop
    spec:
      containers:
        - name: nginx
          image: nginx:latest